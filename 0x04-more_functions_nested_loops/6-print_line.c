#include "main.h"

/**
 * print_line - checks for checks for a digit(0 through 9).
 * @n: n - variable
 * Return: Always 0.
 */
void print_line(int n)
{
int x;
 {
   for(x = 1; x <= n; x++)
    {
     _putchar(95);
     }
 }
 _putchar('\n');
}
